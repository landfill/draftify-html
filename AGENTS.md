# AGENTS.md — 에이전트 작업 규약

이 저장소에서 작업하는 모든 에이전트(어떤 세션·머신·에이전트 종류든)를 위한 지침이다.
목표는 **세션이 바뀌어도 맥락 손실 없이 이어서 작업**하는 것.

## 0. 핵심 원칙 — 맥락은 저장소 안에만 남긴다

에이전트의 개인 메모리(Claude Code의 `~/.claude/.../memory/` 등)는 세션·머신·에이전트
종류가 달라지면 **접근할 수 없다.** 따라서 이 프로젝트에 관한 진행 상태·결정·다음 할 일·
막힌 지점은 **전부 저장소 안 파일에 기록한다.** 개인 메모리에는 의존하지 않는다.

핸드오프에 쓰는 저장소 내 경로:

| 파일 | 역할 |
|------|------|
| `AGENTS.md` (이 파일) | 작업 규약. 어떻게 일하고 무엇을 어디에 기록할지 |
| `docs/PROGRESS.md` | 살아있는 진행 로그 — 현재 단계, WBS 체크리스트, 세션 로그. **작업 시작 시 먼저 읽고, 끝날 때 갱신** |
| `docs/` 스펙 문서 | 무엇을 만들지의 계약. **정본 목록은 아래 §1** |
| `guide/` 원본 문서 | 결정의 근거·원본. **정본 목록은 아래 §1-9.** 스펙과 충돌 시 §4 절차로 해소 |

> **개수를 세지 않는다.** 이 표와 §1은 문서가 늘 때 함께 늘어난다. "3종"·"6종" 같은 개수
> 표기는 목록이 자라면 곧 거짓이 되므로(#64의 계기) **목록 자체를 정본으로 삼는다.**

## 1. 먼저 읽을 문서 (읽는 순서)

0. **원격 동기화 — 문서를 읽기 전에 저장소부터 최신으로 만든다.** `git fetch origin` 후
   로컬 main이 `origin/main`보다 뒤처져 있으면(`git rev-list --count main..origin/main`이 0이 아니면)
   먼저 동기화한다(`git pull --ff-only`). 다른 머신·다른 에이전트(클라우드 세션 포함)가
   병합한 결과가 원격에만 있을 수 있다 — 낡은 클론에서 읽은 `PROGRESS.md`는 낡은 진실이고,
   낡은 base에서 딴 브랜치는 불필요한 충돌을 만든다
1. `docs/PROGRESS.md` — **지금 어디까지 됐는지.** 항상 여기서 시작한다
2. `docs/README.md` — 문서 지도
3. `docs/user-guide.md` — 세 경로(A·B·D)를 사용자가 실제로 어떻게 쓰는지. **제품의 현재 모습**
4. `docs/PRD.md` — 제품 정의·용어·설계 제1원칙·기능 요구사항
5. `docs/detailed-spec.md` — 화면별 상세, 제품 정책(POL-M\*), 수용 기준
6. `docs/technical-spec.md` — 아키텍처·스택·데이터 모델·API·알고리즘·WBS
7. `docs/output-standard.md` — 산출물 HTML의 ID 체계·섹션 구조
8. `docs/implementation-decisions.md` — 미정의 지점 확정 (ID-01~15)
9. `guide/` 원본 — 위 문서들의 근거. 트랙별 킥오프가 계약이고, RFC는 그 근거다:
   - `mockup-as-spec-guide.md` — 코어 개념 (장면·앵커·캡처)
   - `mockup-as-spec-service-architecture.md` — 서비스 구성 (온보딩 경로, 컴포넌트, S1~S3)
   - `s1-kickoff-spec.md` — S1 확정 사양 + **결정 변경 이력(§11)**
   - `s2-kickoff-spec.md` — S2 확정 사양 (경로 B·SSRF·마스킹·CI)
   - `pathD-kickoff-spec.md` — S2.5 확정 사양 (경로 D 확장·프로젝트 토큰)
   - `open-service-kickoff-spec.md` — 공개 멀티테넌트 개편 착수 계약 (Vercel + Supabase)
   - `open-service-rfc.md` — 위 킥오프의 근거 RFC (대안 비교·리뷰 반영 이력)

   각 문서의 지위·재검토 대상 여부는 `docs/README.md` "원 결정 문서 (guide/)"에 있다.

구현을 시작하기 전에 최소한 `PROGRESS.md` · `PRD.md` · `technical-spec.md` ·
`implementation-decisions.md`는 읽는다. (번호가 아니라 파일명으로 적는다 — 목록이 늘면
번호가 어긋난다.)

## 2. 설계 제1원칙 (이탈 금지)

`docs/PRD.md` §1.3의 4개 원칙은 모든 구현 판단의 상위 규범이다:

1. **의도는 사람이 입력한다** — 크롤링·LLM으로 추론하지 않는다. LLM 초안 제안은 옵트인이며 기본 OFF, OFF 시 호출 코드 경로 자체가 비활성
2. **사용자는 목업을 수정하지 않는다** — SDK 주입은 서비스가 대신한다 (`data-spec-id` 같은 소스 오염 금지)
3. **편집은 라이브, 산출물은 캡처** — 캡처는 클라이언트 브라우저에서만, 서버 헤드리스 재현 금지
4. **산출물은 단독 HTML 하나** — file://로 열리고 네트워크 요청 0건

추가로 구현 전반의 판단 기준: **구현이 단순하고 범용적일 것** (`implementation-decisions.md` 서문).
더 정교한 대안이 떠오르면, 그 대안을 버리는 이유가 문서에 있는지 먼저 확인한다.

## 3. 진행 상태 갱신 규약 (`docs/PROGRESS.md`)

작업을 하나 끝낼 때마다(세션 종료를 기다리지 말 것):

1. **WBS 체크리스트**의 해당 항목 상태를 갱신한다 (`[ ]` → `[x]`, 부분 완료는 메모)
2. **세션 로그** 맨 위에 항목을 추가한다. 형식:
   - `### YYYY-MM-DD — 한 줄 제목`
   - `완료:` 이번에 실제로 끝낸 것 (검증 방법 포함)
   - `다음 할 일:` 다음 세션이 곧바로 이어받을 수 있게 구체적으로
   - `막힌 지점:` 사용자 결정이 필요하거나 미해결인 것 (없으면 "없음")
3. 오늘 날짜는 실제 날짜로 적는다. 상대 표현("어제", "내일") 금지 — 다음 세션이 오독한다
4. **이 파일은 `main`에 직접 커밋·푸시한다** (§6의 예외 — 브랜치·PR 불필요). 원격에 즉시
   반영돼야 다른 세션이 낡은 상태를 읽지 않는다

## 4. 결정을 바꿀 때 (문서 변경 절차)

스펙과 다르게 구현해야 할 이유가 생기면 **코드부터 고치지 않는다:**

1. **그 결정이 실린 킥오프 문서**를 먼저 수정한다. 어느 문서인지는 무엇을 바꾸는지로 갈린다:
   S1 코어·산출물 = `s1-kickoff-spec.md`, 경로 B·마스킹 = `s2-kickoff-spec.md`,
   경로 D 확장·프로젝트 토큰 = `pathD-kickoff-spec.md`, 공개판(Vercel·Supabase·RLS) =
   `open-service-kickoff-spec.md`. 걸치면 양쪽 다 고친다
2. **같은 문서의 "결정 변경 이력"** 섹션에 **이유**를 기록한다 (문서마다 절 번호가 다르다)
3. 영향받는 `docs/` 문서를 동기화한다
4. 그런 다음 구현한다

임의 이탈은 다음 세션에게 "문서와 코드 중 뭐가 맞나"라는 혼란을 남긴다. 절대 금지.

## 5. 기술 스택·구조 (요약 — 상세는 technical-spec.md)

- **모노레포**: npm workspaces (turbo/nx 도입 금지). Node 20+, TypeScript strict 전 패키지 공통
- **패키지**: `shared`(타입 계약, 유일 소스) → `sdk`(Preact+Shadow DOM, Vite IIFE 단일 `sdk.js`) / `server`(Express 5) / `viewer`(vanilla TS)
- **저장**: 파일 기반 JSON (프로젝트당 1파일) + 디스크 asset store. 모든 상태는 `data/`(gitignore) 아래에만
- **캡처**: `single-file-core`(npm) 래핑. 직접 구현 금지
- **테스트**: vitest(unit·API) + Playwright(E2E 1본 = S1 DoD)
- **오리진 하드코딩 금지** (ID-01): 클라이언트는 상대 경로만, 서버는 Host 헤더 파싱 + `PORT` env
- **지원 브라우저** (ID-02): 편집 환경은 Chrome/Edge 최신 한정. 뷰어는 표준 API만

## 6. Git 브랜치·커밋 규칙

코드 변경은 **반드시 브랜치를 분리**해서 작업한다. `main`에 직접 커밋하지 않는다.

### 예외 — 핸드오프 문서는 `main`에 직접 커밋한다 (2026-07-31 결정)

**`docs/PROGRESS.md` 갱신만은 브랜치·PR 없이 `main`에 직접 커밋하고 푸시한다.**

- 이 파일은 리뷰 대상이 아니라 **다음 세션이 읽을 상태 기록**이다. PR을 거치면 병합 전까지
  원격에 반영되지 않아, 다른 머신·클라우드 세션이 **낡은 진실을 읽는다** — 핸드오프의 목적과 정면으로 어긋난다
- 병합 순서 사고의 주된 원인도 이것이었다: 문서 PR이 둘 이상 열리면 같은 hunk를 건드려
  서로를 충돌시킨다(2026-07-31 세션 로그의 #66·#67 사례). 직접 커밋이면 그 부류가 사라진다
- **적용 범위는 `docs/PROGRESS.md` 한 파일.** 스펙 6종·`guide/`·`AGENTS.md`·`README.md`는
  종전대로 브랜치 + PR을 거친다(내용이 계약이고 리뷰 가치가 있다)
- 코드 변경과 **같은 커밋에 섞지 않는다.** 코드가 딸린 작업이면 코드는 토픽 브랜치로 가고,
  PROGRESS는 그 브랜치 안에서 함께 커밋해도 되고 병합 후 `main`에 직접 커밋해도 된다
- 직접 커밋 전에는 `git pull --ff-only`로 원격을 먼저 따라잡는다 (§1.0과 같은 이유)

### 브랜치 네이밍 (변경 성격이 기준)

| 접두 | 언제 | 예 |
|------|------|-----|
| `fix/` | **버그 픽스** — 기존에 되던 것이 안 되는 것을 되게 함 | `fix/anchor-refind-empty-text` |
| `feat/` | **기능(피처)** — 없던 동작을 새로 추가함 | `feat/scene-freeze` |
| `docs/` | 문서만 변경 (스펙·PROGRESS·가이드) | `docs/handoff-structure` |
| `chore/` | 빌드·설정·의존성 등 제품 동작과 무관한 정비 | `chore/npm-workspaces-setup` |

- **버그와 기능을 한 브랜치에 섞지 않는다.** 성격이 다르면 브랜치를 나눈다
- 브랜치명 뒤는 kebab-case로 무엇을 건드리는지 짧게. WBS 작업이면 접미에 `-t3`처럼 붙여도 좋다
- 판단이 애매하면(리팩터링이 버그도 고치는 경우 등) 주 목적을 접두로 삼고, 커밋 메시지에 나머지를 적는다

### 커밋

- 커밋은 논리 단위로. 한 커밋에 무관한 변경을 섞지 않는다
- 커밋/푸시는 사용자가 요청할 때만 수행한다. 아직 git 저장소가 아니라면 초기화 여부를 먼저 확인
- 임시 파일은 커밋하지 않는다. `data/`는 gitignore 대상
- **커밋 메시지에 co-author 트레일러를 넣지 않는다** (`Co-Authored-By:`, `Claude-Session:` 등). 에이전트 서명·세션 링크를 커밋 본문에 남기지 않는다

### main 병합 — 사용자 동의 필수

- **`main`으로의 병합(merge/PR merge/fast-forward 포함)은 사용자의 명시적 동의 없이 절대 하지 않는다.**
- 작업이 끝나면 브랜치를 병합 준비 상태로 두고, 병합 여부·시점은 사용자에게 확인한다
- 동의는 그 병합 1건에 한정된다. 이전에 "머지해도 된다"고 했다고 이후 병합까지 자동 승인된 것으로 보지 않는다

### 병합 후 정리

`main` 병합이 끝나면 다음을 정리한다 (다음 세션이 죽은 브랜치·낡은 base에서 시작하지 않게):

- **원격 토픽 브랜치 삭제**: 머지된 `feat/`·`fix/`·`docs/`·`chore/` 브랜치는 원격에서 지운다 (`gh pr merge --delete-branch` 또는 GitHub 자동 삭제). **`main`은 삭제하지 않는다** — 현재 장기 브랜치는 `main` 하나뿐이고, 그것이 배포 브랜치다 (`open-service`는 트랙 종료로 2026-07-30 삭제)
- **임시 워크트리 제거 (브랜치 삭제보다 먼저)**: 그 작업용으로만 판 워크트리는 `git worktree remove`로 정리한다. 토픽 브랜치가 워크트리에 체크아웃된 상태면 `git branch -d`가 실패하므로, 워크트리 제거가 로컬 브랜치 삭제에 선행한다. **상시 유지하는 워크트리는 없다** — 사내판을 띄운 채 다른 브랜치를 동시에 돌려야 하는 경우에만 임시로 파고, 끝나면 지운다
- **로컬 정리**: `main`으로 전환한 뒤 머지된 로컬 토픽 브랜치를 삭제하고(`git branch -d`), 로컬 `main`을 원격과 동기화한다(`git fetch origin --prune` → `git pull --ff-only`). §1.0의 "작업 전 동기화"와 짝이다
- **PROGRESS 갱신**: 병합 완료·이슈 종결은 §3 규약대로 `docs/PROGRESS.md`에 기록한다

## 7. 작업 위생

- 하위 에이전트(subagent)를 띄우더라도, 그 에이전트가 남길 결과·결정은 반드시 이 규약대로 `docs/PROGRESS.md`에 회수한다. 하위 에이전트의 개인 메모리에만 남기지 않는다
